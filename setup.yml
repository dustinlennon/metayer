default:

  base_dir: /home/dnlennon/Workspace/repos/metayer

  usethis:
    packages:
      - cli
      - config
      - fs
      - here
      - highr
      - htmltools
      - IRdisplay
      - jsonlite
      - magrittr
      - purrr
      - readr
      - rlang 
      - stringr
      - withr
      - xfun
      - yaml
    
    import_from:
      - package: magrittr
        fun: "%>%"

    vignettes:
      - authoring

  workflow_srcs:

